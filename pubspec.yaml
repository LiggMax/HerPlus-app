name: herplus_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0


flutter:

  uses-material-design: true

  assets:
    - assets/images/data_privacy/
    - assets/images/firmware_upgrade/
    - assets/images/ai_chat/
    - assets/images/goal_settings/
    - assets/images/gender_cognition/
    - assets/images/tracking_intent/
    - assets/images/cycle_details/
